#include <iostream>  // This allows us to use input/output features

using namespace std; // So we don’t have to write std:: every time

int main() {
    cout << "Hello, World!" << endl;  // Prints Hello, World! to the screen
    return 0;  // Ends the program successfully
}
